<IfModule mod_rewrite.c>
  RewriteEngine On
  # Nếu request đã trỏ vào /public thì không rewrite nữa (tránh public/public/...)
  RewriteCond %{REQUEST_URI} !/public/ [NC]
  RewriteRule ^$ public/ [L]
  RewriteRule ^(.*)$ public/$1 [L]
</IfModule>