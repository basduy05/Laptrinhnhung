#include "config.h"

// Define keypad pin arrays (compiled once, linked to all modules)
const uint8_t KEYPAD_ROW_PINS[KEYPAD_ROWS] = {22, 23, 24, 30};
const uint8_t KEYPAD_COL_PINS[KEYPAD_COLS] = {26, 27, 28, 29};
